[package]
name = "bevy_garage_light"
version = "0.1.0"
edition = "2021"

[features]

[dependencies]
bevy = { workspace = true, default-features = false }
bevy_garage_car = { workspace = true, features = ["graphics"] }
bevy_rapier3d = { workspace = true }
